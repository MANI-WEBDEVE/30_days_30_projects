/** @type {import('next').NextConfig} */
const nextConfig = {};

// next.config.js

// next.config.js

export default {
    images: {
      domains: ['i.imgflip.com'], // Add the domain here
    },
  };
  